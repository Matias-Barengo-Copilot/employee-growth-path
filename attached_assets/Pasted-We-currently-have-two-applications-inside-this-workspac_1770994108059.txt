We currently have two applications inside this workspace:

1) The root application (built with Replit, Express + Vite).
2) A Next.js application located in /next-reference (copilot-lms), which is already deployed and in production.

The Next.js app:
- Is already live and functioning.
- Uses Neon as the database.
- Uses Drizzle ORM.
- Uses Next.js API routes.
- Has production data that must NOT be lost.
- Contains a "Members" section that serves a similar purpose to the "Directory" feature in the Replit app.
- Was originally created to handle the "Time Off" functionality from the Replit app.

The root Replit app contains additional features that are NOT yet implemented in the Next.js app.

The main objective:
We want to KEEP the Next.js app as the primary production application and progressively integrate the missing features from the Replit app into it, using scalable architecture and best practices.

After the migration is complete:
- We want to continue working from Replit.
- But the active project should be the Next.js app.
- With all missing features properly integrated.
- Without breaking production.
- Without losing Neon database data.
- Without breaking Drizzle schema integrity.
- Without breaking role/permission logic.

Your tasks:

1) Perform a full architectural comparison between:
   - The root Replit app
   - The Next.js app inside /next-reference

2) Analyze:
   - How Directory relates to Members
   - How Time Off was implemented in both
   - Differences in backend architecture (Express vs Next API routes)
   - Differences in state management
   - Differences in authentication and role systems

3) Evaluate the role/permission systems carefully and determine:
   - How roles are currently enforced in both systems
   - What changes would be required to safely merge missing features
   - How to avoid privilege escalation or logic conflicts

4) Propose a safe migration strategy that:
   - Keeps the Next.js app as the base
   - Migrates features incrementally
   - Preserves the existing Neon database
   - Preserves Drizzle schema integrity
   - Avoids breaking production

5) Identify:
   - Which features should be migrated first
   - Which backend logic needs to be rewritten
   - Which parts can be reused
   - Which parts should be refactored

6) If necessary, explicitly request the .env file of the Next.js app and explain:
   - Why you need it
   - What variables are critical
   - What risks exist if misconfigured

IMPORTANT:
Do NOT modify any files yet.

Instead:
Generate a comprehensive migration strategy document in Markdown format named:

MIGRATION_PLAN_NEXT_INTEGRATION.md

This document must:
- Be structured step-by-step
- Include architectural diagrams (in text form if needed)
- Include risk analysis
- Include rollback strategy
- Include database safety considerations
- Include role/permission mapping strategy
- Include a phased execution plan

This document will be our single source of truth to execute the migration safely and systematically.

We are using Plan Mode, so think deeply before proposing changes.
Do not rush to implementation.
Focus on correctness, scalability, and production safety.
